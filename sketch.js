// Major Project
// p5Play engine


let car, cone, road;

function setup() {
  new Canvas(windowWidth, windowHeight);

  car = new Sprite(windowWidth/2, windowHeight - windowHeight/4, 50, 100);

  
  

}

function draw() {
  clear();
}