// Major Project
// p5Play engine


let box, floor;

function setup() {
  new Canvas(windowWidth, windowHeight);

  box = new Sprite();
  box.x = 200;
  box.width = 50;
  box.height = 50;
  
  floor.width = width;
  floor.y = 50;
  

}

function draw() {
  clear();
}